# auto-generated by grc.converter

id: rds_parser
label: RDS Parser
category: '[Core]/Encode+Decode'

parameters:
-   id: log
    label: Log
    dtype: bool
    default: 'False'
    options: ['True', 'False']
    option_labels: [Enable, Disable]
-   id: debug
    label: Debug
    dtype: bool
    default: 'False'
    options: ['True', 'False']
    option_labels: [Enable, Disable]
-   id: pty_locale
    label: PTY Locale
    dtype: int
    default: '0'
    options: ['0', '1']
    option_labels: [Europe, North America]
-   id: reset
    label: Reset
    dtype: raw
    default: '0'
    hide: part

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out
    optional: true

templates:
    imports: import rds
    make: rds.parser(${log}, ${debug}, ${pty_locale})
    callbacks:
    - reset(${reset})

documentation: |-
    ######################################################
    ######################################################
    Open rds_rx.grc example flow graph in GNU Radio Companion.

    Quick example:
    http://www.youtube.com/watch?v=05i9C5lhorY

    HAK5 episode (including installation):
    http://www.youtube.com/watch?v=ukhrIl4JHbw

    FOSDEM'15 talk (video and slides):
    https://archive.fosdem.org/2015/schedule/event/sdr_rds_tmc/
    ######################################################
    ######################################################

file_format: 1
